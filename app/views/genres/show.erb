
<h1><%=@genre.name%></h1>

<h3>Song(s): <%@genre.songs.each do |song|%>
    <a href="/songs/<%=song.slug%>"><%= song.name %></a>
    <%end%></h3>

<h3>Artists(s): <%@genre.artists.each do |artist|%>
    <a href="/artists/<%=artist.slug%>"><%= artist.name %></a>
    <%end%></h3>